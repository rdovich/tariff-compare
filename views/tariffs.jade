extends layout

block content
  div.span12
    h2 #{Subtitle}
    form.well.form-inline(method='post', action='/display-tariffs')
      label Select your tariff for #{utilityName}:
      select(name='tariff_list')
        for tariff in tariffs
          option #{tariff.tariffName}
      button.btn(type='submit').
        Get Next Thing